import { FiltroPipe } from 'src/app/cmp05-pipes/pipes/filtro/filtro.pipe';

describe('FiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
