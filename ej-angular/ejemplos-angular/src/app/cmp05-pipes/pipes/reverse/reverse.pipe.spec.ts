import { ReversePipe } from 'src/app/cmp05-pipes/pipes/reverse/reverse.pipe';

describe('ReversePipe', () => {
  it('create an instance', () => {
    const pipe = new ReversePipe();
    expect(pipe).toBeTruthy();
  });
});
