import { DoblePipe } from 'src/app/cmp05-pipes/pipes/doble/doble.pipe';

describe('DoblePipe', () => {
  it('create an instance', () => {
    const pipe = new DoblePipe();
    expect(pipe).toBeTruthy();
  });
});
