import { OcultarDigitosPipe } from 'src/app/tarjetas-credito/pipes/ocultar-digitos/ocultar-digitos.pipe';

describe('OcultarDigitosPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarDigitosPipe();
    expect(pipe).toBeTruthy();
  });
});
